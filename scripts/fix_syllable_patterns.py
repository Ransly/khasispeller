#!/usr/bin/env python3
"""
WITHDRAWN — do not run. Superseded and then reversed.

A two-row table that forced `tiew` -> CVVC and `ïang` -> VVCC across the
lexicon on 2026-09-10. Both readings are orthographic — they count `w` as a
coda consonant and split `ng` into two — and both were reversed the same day
along with the fuller pass that generalised them.

See `recompute_syllable_patterns.py` in this directory for the reasoning,
the evidence from the stored IPA, and what the CV pattern is actually used
for (display only).
"""
print(__doc__)
print("This script is withdrawn and will not run.")
raise SystemExit(2)
